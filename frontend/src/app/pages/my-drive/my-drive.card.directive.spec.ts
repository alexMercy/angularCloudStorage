import { MyDriveCardDirective } from './my-drive.card.directive';

describe('MyDriveCardDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDriveCardDirective();
    expect(directive).toBeTruthy();
  });
});
